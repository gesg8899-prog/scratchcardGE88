<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Premium Casino Scratch Card</title>
  <style>
    body {
      margin: 0;
      font-family: 'Arial Black', Arial, sans-serif;
      background: linear-gradient(135deg, #1a1a1a, #0f0f0f); /* Flat elegant background */
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .main-container {
      width: 400px;
      aspect-ratio: 1;
      background: linear-gradient(135deg, #2b0000, #4d0000);
      border-radius: 30px;
      padding: 30px;
      box-shadow: 0 0 30px rgba(255, 215, 0, 0.4); /* gold glow */
      border: 3px solid gold;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .header-section {
      text-align: center;
      margin-bottom: 20px;
    }

    .main-title {
      font-size: 32px;
      font-weight: 900;
      color: gold;
      margin-bottom: 8px;
    }

    .casino-brand {
      font-size: 18px;
      font-weight: bold;
      color: #ffffff;
      margin-bottom: 4px;
    }

    .tagline {
      font-size: 14px;
      font-weight: bold;
      color: #ffffff;
    }

    .scratch-card {
      width: 100%;
      height: 280px;
      position: relative;
      border-radius: 25px;
      overflow: hidden;
      border: 4px solid #800000;
      box-shadow: 0 0 40px rgba(128,0,0,0.4), inset 0 0 30px rgba(0,0,0,0.5);
      background: radial-gradient(circle, #1a0f00, #2d1f00, #1a0f00);
      margin-bottom: 20px;
    }

    .prize-area {
      position: absolute;
      inset: 0;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      z-index: 5;
    }

    .prize-emoji {
      font-size: 80px;
      margin-bottom: 15px;
      color: gold;
      filter: drop-shadow(0 0 20px rgba(255, 215, 0, 0.8));
    }

    .prize-amount {
      font-size: 28px;
      font-weight: 900;
      color: #ffd700;
      text-align: center;
      letter-spacing: 2px;
      text-shadow: 0 0 20px rgba(255, 215, 0, 0.8);
    }

    #scratchCanvas {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      cursor: crosshair;
      border-radius: 21px;
      z-index: 10;
    }

    .action-area {
      display: flex;
      justify-content: center;
      margin-top: 10px;
    }

    .reveal-button {
      background: linear-gradient(135deg, #800000, #8b0000);
      border: none;
      padding: 15px 35px;
      border-radius: 30px;
      font-weight: 900;
      font-size: 16px;
      color: #ffffff;
      cursor: pointer;
      transition: all 0.3s ease;
      box-shadow: 0 6px 20px rgba(128,0,0,0.4);
      border: 2px solid #ffffff;
      letter-spacing: 1px;
    }

    .reveal-button:hover {
      transform: translateY(-3px);
      box-shadow: 0 10px 30px rgba(128,0,0,0.6);
      background: linear-gradient(135deg, #8b0000, #a0522d);
    }

    .footer-text {
      text-align: center;
      margin-top: 15px;
      color: #ffd700;
      font-size: 14px;
      font-weight: bold;
      letter-spacing: 1px;
    }

    /* Responsive */
    @media (max-width: 500px) {
      .main-container { width: 95vw; padding: 20px; }
      .main-title { font-size: 28px; }
      .prize-emoji { font-size: 60px; }
      .prize-amount { font-size: 22px; }
      .scratch-card { height: 220px; }
    }
  </style>
</head>
<body>
  <div class="main-container">
    <div class="header-section">
      <div class="main-title">🎰 SCRATCH & WIN 🎰</div>
      <div class="casino-brand">GE88 CASINO</div>
      <div class="tagline">⚡ PREMIUM JACKPOT EXPERIENCE ⚡</div>
    </div>

    <div class="scratch-card">
      <div class="prize-area">
        <div class="prize-emoji" id="prize-emoji">🪙</div>
        <div class="prize-amount" id="prize-amount">WIN 6 SGD</div>
      </div>
      <canvas id="scratchCanvas"></canvas>
    </div>

    <div class="action-area">
      <button class="reveal-button" onclick="revealAll()">🔥 REVEAL ALL 🔥</button>
    </div>

    <div class="footer-text">✨ SCRATCH THE GOLD TO REVEAL YOUR PRIZE ✨</div>
  </div>

  <script>
    const canvas = document.getElementById('scratchCanvas');
    const ctx = canvas.getContext('2d');
    let isScratching = false;
    let previousX = 0;
    let previousY = 0;

    function resizeCanvas() {
      const rect = canvas.getBoundingClientRect();
      canvas.width = rect.width * 2;
      canvas.height = rect.height * 2;
      ctx.scale(2,2);
      createScratchSurface();
    }

    function createScratchSurface() {
      ctx.fillStyle = '#808080'; // gray scratch layer
      ctx.fillRect(0, 0, canvas.width/2, canvas.height/2);
    }

    function performScratch(e) {
      const rect = canvas.getBoundingClientRect();
      const x = (e.clientX || e.touches[0].clientX) - rect.left;
      const y = (e.clientY || e.touches[0].clientY) - rect.top;

      ctx.globalCompositeOperation = "destination-out";
      ctx.lineWidth = 25;
      ctx.lineCap = "round";

      ctx.beginPath();
      ctx.moveTo(previousX || x, previousY || y);
      ctx.lineTo(x, y);
      ctx.stroke();

      previousX = x;
      previousY = y;
    }

    function revealAll() {
      ctx.clearRect(0,0,canvas.width, canvas.height);
    }

    canvas.addEventListener('mousedown', () => isScratching = true);
    canvas.addEventListener('mouseup', () => { isScratching = false; previousX=0; previousY=0; });
    canvas.addEventListener('mousemove', (e) => { if(isScratching) performScratch(e); });

    canvas.addEventListener('touchstart', (e) => { e.preventDefault(); isScratching = true; performScratch(e); });
    canvas.addEventListener('touchmove', (e) => { e.preventDefault(); if(isScratching) performScratch(e); });
    canvas.addEventListener('touchend', () => { isScratching=false; previousX=0; previousY=0; });

    window.addEventListener('resize', resizeCanvas);
    window.addEventListener('load', resizeCanvas);
  </script>
</body>
</html>
